import { AbstractShape } from "../shape/AbstractShape";

export function onRemoveVertexMouseDown(
    e: MouseEvent,
    shape: AbstractShape
) {
    if (shape.type === "polygon") {
        // 
    }
}
